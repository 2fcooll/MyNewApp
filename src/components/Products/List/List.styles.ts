import { StyleSheet } from "react-native";
import { w } from "../../../styles/scale";


const styles = StyleSheet.create({
    wrapper: {

    },

    image: {
        // width: '100%',
        height: w(336.95),
        borderRadius: w(10),
    }
});

export { styles };